#pragma once
#ifndef CALCULATOR_MATH_HPP
#define CALCULATOR_MATH_HPP
#include <iostream>
#include <string>
#include <memory>
#include <vector>
using namespace std;

pair<string, string> askForNum();

void calculate(vector<string> dynamicStringArray);

#endif // !CALCULATOR_MATH_HPP